name: Deploy to Render

on:
  push:
    branches:
      - main  # Runs when you push to main branch

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deployment
        run: |
          curl "https://api.render.com/deploy/srv-d3f864h5pdvs73ckl6f0?key=tPqB6XmML3E"
        env:
          RENDER_DEPLOY_HOOK: "https://api.render.com/deploy/srv-d3f864h5pdvs73ckl6f0?key=tPqB6XmML3E"
